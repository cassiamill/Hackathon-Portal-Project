import React from 'react';
import ReactDOM from 'react-dom/client';

// This is where the app starts
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<h1>Testing hackathon frontend</h1>);
